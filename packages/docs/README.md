# @myorg — API Reference

This site is auto-generated from the TypeScript source of all published packages.

## Packages

| Package | Description |
|---|---|
| `@myorg/types` | Shared TypeScript interfaces — entities, API routes, auth context |
| `@myorg/ui`    | Shared MUI component library — PageHeader, StatusBadge, LoadingPane, ErrorPane |

## Related

- **Storybook** — available at `/storybook/` on the deployed Pages site
- **Admin app** — the main React Admin shell

## Regenerating

```bash
# From repo root:
pnpm --filter=@myorg/docs build

# Watch mode during development:
pnpm --filter=@myorg/docs dev
```
